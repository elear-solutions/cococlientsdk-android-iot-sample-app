<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="buzz.getcoco.iot.sample">

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="false"
    android:theme="@style/Theme.IoTSampleApp">

    <meta-data
      android:name="buzz.getcoco.auth.client_id"
      android:value="your_client_id_here" />

    <activity
      android:name="buzz.getcoco.iot.sample.activities.SplashActivity"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <activity
      android:name="buzz.getcoco.iot.sample.activities.CocoNetworksActivity"
      android:exported="true" />

    <activity
      android:name="buzz.getcoco.iot.sample.activities.MainActivity"
      android:exported="true">
    </activity>

    <activity
      android:name="buzz.getcoco.auth.CocoRedirectActivity"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>
        <data android:scheme="your_redirect_scheme_here"/>
      </intent-filter>
    </activity>
  </application>
</manifest>
